# راهنمای کامل تست ماژول مدیریت چک (Cheque Management)

## 📋 فهرست مطالب
1. [پیش‌نیازها](#پیش‌نیازها)
2. [آماده‌سازی اولیه](#آماده‌سازی-اولیه)
3. [Workflow چک‌های دریافتنی](#workflow-چک‌های-دریافتنی)
4. [Workflow چک‌های پرداختنی](#workflow-چک‌های-پرداختنی)
5. [تست گام به گام](#تست-گام-به-گام)
6. [تست از طریق UI](#تست-از-طریق-ui)
7. [تست از طریق کد](#تست-از-طریق-کد)

---

## پیش‌نیازها

### 1. نصب و راه‌اندازی
```bash
# اطمینان حاصل کنید که migrate انجام شده است
bench --site mysite.localhost migrate

# Clear cache
bench --site mysite.localhost clear-cache
```

### 2. ایجاد حساب‌های مورد نیاز در Chart of Accounts

قبل از استفاده، باید حساب‌های زیر را ایجاد کنید:

**برای چک‌های دریافتنی:**
- **Receivable Cheque Account** (Account Type: `Asset`)
  - مثال: `Assets > Current Assets > Receivable Cheque`
  
- **Under Collection Account** (Account Type: `Asset`)
  - مثال: `Assets > Current Assets > Under Collection`
  
- **Returned Cheque Account** (Account Type: `Asset`)
  - مثال: `Assets > Current Assets > Returned Cheque`

**برای چک‌های پرداختنی:**
- **Payable Cheque Account** (Account Type: `Liability`)
  - مثال: `Liabilities > Current Liabilities > Payable Cheque`

**حساب بانکی:**
- **Bank Account** (Account Type: `Bank`)
  - مثال: `Assets > Bank Accounts > Main Bank`

### 3. ایجاد Company (در صورت عدم وجود)
- حداقل یک Company باید وجود داشته باشد
- حداقل یک Customer و یک Supplier باید وجود داشته باشد

---

## آماده‌سازی اولیه

### مرحله 1: ایجاد Cheque Settings

#### روش 1: از طریق UI
1. به `http://mysite.localhost:8000` بروید
2. وارد سیستم شوید
3. به **Cheque Management > Cheque Settings** بروید
4. Company را انتخاب کنید
5. حساب‌های پیش‌فرض را تنظیم کنید:
   - Default Receivable Cheque Account
   - Default Under Collection Account
   - Default Returned Cheque Account
   - Default Payable Cheque Account
   - Default Bank Account
6. در صورت نیاز، "Enable Sayad Fields" را فعال کنید
7. **Save** کنید

#### روش 2: از طریق کد (Console)
```python
# در Frappe Console
bench --site mysite.localhost console

import frappe
from erpnext_extensions.cheque_management.demo_data import setup_demo_data

# جایگزین کنید با نام Company خود
setup_demo_data("Your Company Name")
```

---

## Workflow چک‌های دریافتنی (Receivable)

سیستم از **Status-based workflow** استفاده می‌کند (نه Frappe Workflow). وضعیت‌ها به صورت زیر هستند:

```
┌─────────────────────────┐
│ Received From Customer  │ ← وضعیت اولیه
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Waiting For Sayad      │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│ Registered In Sayad     │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│      Move To Box        │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│    Under Collection     │ ← ایجاد JE: Under Collection
└───────────┬─────────────┘
            │
            ├─────────────────┐
            ▼                 ▼
┌──────────────────────┐  ┌──────────────────────┐
│     Collected        │  │ Returned From Bank   │ ← ایجاد JE: Returned
│   (ایجاد JE)         │  │   (ایجاد JE)         │
└──────────────────────┘  └──────────┬───────────┘
                                     │
                                     ▼
                            ┌──────────────────────┐
                            │   Return To Customer │
                            └──────────────────────┘
```

### وضعیت‌ها و توضیحات:

| وضعیت | توضیحات | ایجاد JE |
|------|---------|----------|
| **Received From Customer** | چک از مشتری دریافت شده | ❌ |
| **Waiting For Sayad** | در انتظار ثبت در سیستم صیاد | ❌ |
| **Registered In Sayad** | در سیستم صیاد ثبت شد | ❌ |
| **Move To Box** | چک به صندوق منتقل شد | ❌ |
| **Under Collection** | چک به بانک ارسال شد (در حال وصول) | ✅ Under Collection JE |
| **Collected** | چک وصول شد | ✅ Collection JE |
| **Returned From Bank** | چک از بانک برگشت | ✅ Return JE |
| **Return To Customer** | چک به مشتری برگشت داده شد | ❌ |
| **Cancelled** | چک لغو شد | ❌ |

---

## Workflow چک‌های پرداختنی (Payable)

```
┌──────────────────────────┐
│ Payment Request Created  │ ← وضعیت اولیه
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────┐
│      Select Bank         │
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────┐
│        Issued            │ ← ایجاد JE: Payable Issue
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────┐
│    Mark As Printed       │
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────┐
│  First Signature Done    │
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────┐
│ Second Signature Done    │
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────┐
│    Notify Supplier       │
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────┐
│  Deliver To Supplier     │
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────┐
│ Registered In Sayad      │
└───────────┬──────────────┘
            │
            ▼
┌──────────────────────────┐
│    Sayad Success         │
└──────────────────────────┘
```

### وضعیت‌ها و توضیحات:

| وضعیت | توضیحات | ایجاد JE |
|------|---------|----------|
| **Payment Request Created** | درخواست پرداخت ایجاد شد | ❌ |
| **Select Bank** | حساب بانکی انتخاب شد | ❌ |
| **Issued** | چک صادر شد | ✅ Payable Issue JE |
| **Mark As Printed** | چک چاپ شد | ❌ |
| **First Signature Done** | امضای اول انجام شد | ❌ |
| **Second Signature Done** | امضای دوم انجام شد | ❌ |
| **Notify Supplier** | تامین‌کننده اطلاع داده شد | ❌ |
| **Deliver To Supplier** | چک به تامین‌کننده تحویل شد | ❌ |
| **Registered In Sayad** | در سیستم صیاد ثبت شد | ❌ |
| **Sayad Success** | ثبت در صیاد موفق بود | ❌ |
| **Mark As Void** | چک باطل شد (فقط Cheque Manager) | ❌ |
| **Cancelled** | چک لغو شد | ❌ |

---

## تست گام به گام

### تست 1: چک دریافتنی (Receivable) - از طریق UI

#### گام 1: ایجاد چک دریافتنی
1. به **Cheque Management > Cheque** بروید
2. روی **New** کلیک کنید
3. فرم را پر کنید:
   - **Cheque Type**: `Receivable`
   - **Company**: انتخاب کنید
   - **Cheque No**: `TEST-REC-001`
   - **Cheque Date**: تاریخ امروز
   - **Cheque Amount**: `1000000`
   - **Party Type**: `Customer`
   - **Party**: یک Customer انتخاب کنید
4. **Save** کنید
5. ✅ وضعیت باید `Received From Customer` باشد

#### گام 2: Mark Waiting For Sayad
1. روی دکمه **"Mark Waiting For Sayad"** کلیک کنید
2. ✅ وضعیت باید به `Waiting For Sayad` تغییر کند

#### گام 3: Mark Registered In Sayad
1. روی دکمه **"Mark Registered In Sayad"** کلیک کنید
2. ✅ وضعیت باید به `Registered In Sayad` تغییر کند

#### گام 4: Move To Box
1. روی دکمه **"Move To Box"** کلیک کنید
2. ✅ وضعیت باید به `Move To Box` تغییر کند

#### گام 5: Assign To Bank (ایجاد JE)
1. روی دکمه **"Assign To Bank"** کلیک کنید
2. یک Dialog باز می‌شود برای انتخاب Posting Date
3. تاریخ را انتخاب کنید و **OK** کنید
4. ✅ وضعیت باید به `Under Collection` تغییر کند
5. ✅ یک پیام موفقیت نشان داده می‌شود با لینک Journal Entry
6. ✅ یک Journal Entry ایجاد شده و Submit شده است
7. ✅ در جدول **Journal References** یک رکورد اضافه شده است

**بررسی Journal Entry:**
- به Journal Entry ایجاد شده بروید
- Debit: `Default Receivable Cheque Account` به مبلغ چک
- Credit: `Default Under Collection Account` به مبلغ چک

#### گام 6: Mark As Collected (فقط Cheque Manager)
1. روی دکمه **"Mark As Collected"** کلیک کنید
2. Dialog باز می‌شود برای انتخاب Posting Date و Bank Account
3. اطلاعات را پر کنید و **OK** کنید
4. ✅ وضعیت باید به `Collected` تغییر کند
5. ✅ یک Journal Entry جدید ایجاد شده است

**بررسی Journal Entry:**
- Debit: `Default Bank Account` به مبلغ چک
- Credit: `Default Under Collection Account` به مبلغ چک

#### گام 7: Mark As Returned From Bank (سناریوی برگشت چک)
اگر چک برگشت خورده:
1. چک را در وضعیت `Under Collection` قرار دهید
2. روی دکمه **"Mark As Returned From Bank"** کلیک کنید
3. ✅ وضعیت باید به `Returned From Bank` تغییر کند
4. ✅ یک Journal Entry ایجاد شده است

#### گام 8: Return To Customer
1. روی دکمه **"Return To Customer"** کلیک کنید
2. ✅ وضعیت باید به `Return To Customer` تغییر کند

---

### تست 2: چک پرداختنی (Payable) - از طریق UI

#### گام 1: ایجاد چک پرداختنی
1. به **Cheque Management > Cheque** بروید
2. روی **New** کلیک کنید
3. فرم را پر کنید:
   - **Cheque Type**: `Payable`
   - **Company**: انتخاب کنید
   - **Cheque No**: `TEST-PAY-001`
   - **Cheque Date**: تاریخ آینده (مثلاً 30 روز بعد)
   - **Cheque Amount**: `500000`
   - **Party Type**: `Supplier`
   - **Party**: یک Supplier انتخاب کنید
   - **Bank Account**: یک حساب بانکی انتخاب کنید
4. **Save** کنید
5. ✅ وضعیت باید `Payment Request Created` باشد

#### گام 2: Select Bank
1. اگر Bank Account انتخاب نشده، آن را انتخاب کنید
2. روی دکمه **"Select Bank"** کلیک کنید
3. ✅ وضعیت باید به `Select Bank` تغییر کند

#### گام 3: Issue Cheque (ایجاد JE)
1. روی دکمه **"Issue Cheque"** کلیک کنید
2. Dialog برای Posting Date باز می‌شود
3. تاریخ را انتخاب کنید و **OK** کنید
4. ✅ وضعیت باید به `Issued` تغییر کند
5. ✅ یک Journal Entry ایجاد شده است

**بررسی Journal Entry:**
- Debit: `Default Payable Cheque Account` به مبلغ چک
- Credit: `Default Bank Account` به مبلغ چک

#### گام 4 تا 9: ادامه workflow
1. **Mark As Printed** → وضعیت: `Mark As Printed`
2. **First Signature Done** → وضعیت: `First Signature Done`
3. **Second Signature Done** → وضعیت: `Second Signature Done`
4. **Notify Supplier** → وضعیت: `Notify Supplier`
5. **Deliver To Supplier** → وضعیت: `Deliver To Supplier`
6. **Mark Registered In Sayad** → وضعیت: `Mark Registered In Sayad`
7. **Mark Sayad Success** → وضعیت: `Mark Sayad Success`

---

## تست از طریق کد

### روش 1: استفاده از Demo Functions

```python
# باز کردن Frappe Console
bench --site mysite.localhost console

import frappe
from erpnext_extensions.cheque_management.demo_data import (
    setup_demo_data,
    create_demo_receivable_cheque,
    create_demo_payable_cheque,
    test_receivable_cheque_lifecycle,
    test_payable_cheque_lifecycle
)

# مرحله 1: Setup
company = "Your Company Name"  # جایگزین کنید
setup_demo_data(company)

# مرحله 2: ایجاد و تست چک دریافتنی
receivable = create_demo_receivable_cheque(
    company=company,
    customer="CUST-00001",  # جایگزین کنید
    amount=1000000
)

# تست کامل lifecycle
test_receivable_cheque_lifecycle(receivable.name)

# مرحله 3: ایجاد و تست چک پرداختنی
payable = create_demo_payable_cheque(
    company=company,
    supplier="SUP-00001",  # جایگزین کنید
    amount=500000
)

# تست کامل lifecycle
test_payable_cheque_lifecycle(payable.name)
```

### روش 2: تست دستی Step by Step

```python
import frappe
from erpnext_extensions.cheque_management.utils import ReceivableChequeStatus

# دریافت چک
cheque = frappe.get_doc("Cheque", "CHEQ-2025-00001")

# بررسی وضعیت فعلی
print(f"Current Status: {cheque.status}")

# تغییر وضعیت از طریق متدها
cheque.mark_waiting_for_sayad()
cheque.save()
print(f"New Status: {cheque.status}")  # باید "Waiting For Sayad" باشد

cheque.reload()
cheque.mark_registered_in_sayad()
cheque.save()
print(f"New Status: {cheque.status}")  # باید "Registered In Sayad" باشد

# ادامه workflow...
```

---

## چک‌لیست تست کامل

### ✅ تست‌های اصلی:

- [ ] **Cheque Settings**
  - [ ] می‌توان Cheque Settings ایجاد کرد
  - [ ] حساب‌های پیش‌فرض به درستی تنظیم می‌شوند

- [ ] **چک دریافتنی**
  - [ ] می‌توان چک دریافتنی ایجاد کرد
  - [ ] وضعیت اولیه `Received From Customer` است
  - [ ] تمام دکمه‌های Action کار می‌کنند
  - [ ] Journal Entry برای "Assign To Bank" ایجاد می‌شود
  - [ ] Journal Entry برای "Mark As Collected" ایجاد می‌شود
  - [ ] Journal Entry برای "Mark As Returned" ایجاد می‌شود
  - [ ] Journal References به درستی لینک می‌شوند

- [ ] **چک پرداختنی**
  - [ ] می‌توان چک پرداختنی ایجاد کرد
  - [ ] وضعیت اولیه `Payment Request Created` است
  - [ ] تمام دکمه‌های Action کار می‌کنند
  - [ ] Journal Entry برای "Issue Cheque" ایجاد می‌شود
  - [ ] Journal References به درستی لینک می‌شوند

- [ ] **Permissions**
  - [ ] Cheque User نمی‌تواند "Mark As Collected" را ببیند
  - [ ] Cheque Manager می‌تواند تمام Action‌ها را انجام دهد
  - [ ] کاربر نمی‌تواند Status را به صورت دستی تغییر دهد

- [ ] **Validation**
  - [ ] نمی‌توان Status را به صورت غیرمجاز تغییر داد
  - [ ] نمی‌توان چک با شماره تکراری ایجاد کرد
  - [ ] پیام‌های خطا مناسب نمایش داده می‌شوند

---

## عیب‌یابی (Troubleshooting)

### مشکل: "Cheque Settings not found"
**راه حل:**
```python
from erpnext_extensions.cheque_management.demo_data import setup_demo_data
setup_demo_data("Your Company Name")
```

### مشکل: "Account not found"
**راه حل:** 
- به Chart of Accounts بروید
- حساب‌های مورد نیاز را ایجاد کنید
- در Cheque Settings حساب‌ها را تنظیم کنید

### مشکل: "Permission denied"
**راه حل:**
- مطمئن شوید کاربر دارای Role "Cheque Manager" است
- برای عملیات حساس (Mark As Collected, Mark As Void) نیاز به Cheque Manager است

### مشکل: "Status cannot be changed"
**راه حل:**
- Status را به صورت دستی تغییر ندهید
- از دکمه‌های Action استفاده کنید
- مطمئن شوید که تغییر Status از وضعیت فعلی مجاز است

### مشکل: Journal Entry ایجاد نمی‌شود
**راه حل:**
- بررسی کنید که Cheque Settings تنظیم شده باشد
- بررسی کنید که حساب‌ها در Cheque Settings درست انتخاب شده باشند
- لاگ‌های سرور را بررسی کنید

---

## نکات مهم

1. **Workflow در این ماژول به صورت Status-based است**، نه Frappe Workflow
2. **Status فقط از طریق Action Buttons تغییر می‌کند**، نه دستی
3. **Journal Entries به صورت خودکار Submit می‌شوند**
4. **Journal References به صورت خودکار لینک می‌شوند**
5. **Cheque Manager** برای عملیات حساس مالی نیاز است

---

## مثال کامل تست

```python
# تست کامل در یک نگاه
bench --site mysite.localhost console

exec(open('apps/erpnext_extensions/erpnext_extensions/cheque_management/test_cheque_management.py').read())
```

یا:

```python
from erpnext_extensions.cheque_management.test_cheque_management import run_all_tests
run_all_tests()
```

---

**نویسنده:** Auto  
**تاریخ:** 2025-01-27  
**نسخه:** 1.0

